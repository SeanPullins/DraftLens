# DraftLens local data/model pipeline.
# Install:  python3 -m pip install -r requirements.txt
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
openpyxl>=3.1          # .xlsx / .xls reading
python-dateutil>=2.8

# Optional readers — only needed if your data uses these formats.
pyarrow>=14.0          # .parquet
duckdb>=0.10           # .duckdb
